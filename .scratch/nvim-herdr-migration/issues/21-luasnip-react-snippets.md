# Task: install LuaSnip + port React snippets

Type: task
Status: open

## Question

Per research 16, install LuaSnip via `vim.pack.add` (`jsregexp` not needed for
the Lua-node approach) and port the user's React component snippets, using
`functionNode`/`dynamicNode` to do filename → PascalCase transforms — the Zed
snippet limitation this migration is escaping. Follow the existing snippet
file convention under `.config/nvim/`.
