# Task: enable built-in undotree

Type: task
Status: open

## Question

Per research 11, enable Neovim's built-in `:Undotree` (`:packadd nvim.undotree`,
ships in nvim ≥ 0.12) and bind it to a keymap. Persistent undo (`undofile`) is
already on; this adds the browseable timeline view. Revisit `jiaoshijie/undotree`
only if the diff-against-state view is wanted in daily use.
