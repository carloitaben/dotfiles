repos:
  - repo: local
    hooks:
      - id: prettier
        name: prettier
        language: system
        files: \.tsx?$
        exclude: (^|/)node_modules/
        entry: >-
          bash -c 'prettier="$(npm root)/.bin/prettier"; [ -x "$prettier" ] && "$prettier" --write "$@"; true' --
