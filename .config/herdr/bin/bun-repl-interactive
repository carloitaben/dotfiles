#!/usr/bin/expect -f

set timeout -1
spawn bun repl

# Bun's REPL consumes Escape itself, so intercept it before forwarding input.
interact {
  \033 {
    exec herdr pane close $env(HERDR_PANE_ID)
    exit
  }
}
