#!/bin/sh
set -eu

branch="$1"

if [ "${HERDR_ENV:-}" = "1" ]; then
  herdr worktree create --branch "$branch" --focus >/dev/null
else
  repo_root=$(git rev-parse --show-toplevel)
  path="$(dirname "$repo_root")/$branch"
  git worktree add "$path" "$branch"
fi
