#!/bin/sh
set -eu

branch="$1"
base="$2"

repo_root=$(git rev-parse --show-toplevel)
path="$(dirname "$repo_root")/$branch"

git worktree add "$path" "$base" -b "$branch"

if [ "${HERDR_ENV:-}" = "1" ]; then
  lazygit-worktree-switch "$path"
fi
